diff --git a/public/marketing.html b/public/marketing.html
index ca09de8..7aec985 100644
--- a/public/marketing.html
+++ b/public/marketing.html
@@ -1463,10 +1463,49 @@ const observer = new IntersectionObserver((entries) => {
 document.querySelectorAll('.reveal').forEach(el => observer.observe(el));
 
 // Consultation form submit
-function handleSubmit(btn) {
-  btn.textContent = '✓ Request sent!';
-  btn.style.background = '#166534';
-  setTimeout(() => { btn.textContent = 'Book consultation →'; btn.style.background = ''; goConsultStep(1); }, 4000);
+async function handleSubmit(btn) {
+  const step2 = document.getElementById('subj-step2');
+  const inputs = step2.querySelectorAll('.form-input');
+  const firstName = inputs[0]?.value?.trim();
+  const lastName = inputs[1]?.value?.trim();
+  const email = inputs[2]?.value?.trim();
+  const phone = inputs[3]?.value?.trim() || null;
+  const gradeLevel = inputs[4]?.value?.trim() || null;
+  const goals = inputs[5]?.value?.trim() || null;
+  const marketingConsent = step2.querySelector('.form-checkboxes input[type="checkbox"]')?.checked || false;
+  const province = document.getElementById('subj-province')?.value || null;
+  const selectedSubjects = Array.from(document.querySelectorAll('#subj-container input[type="checkbox"]:checked')).map(cb => cb.value);
+
+  if (!firstName || !lastName || !email) {
+    alert('Please fill in your first name, last name, and email.');
+    return;
+  }
+
+  btn.disabled = true;
+  btn.textContent = 'Sending...';
+
+  try {
+    const res = await fetch('/api/consultations', {
+      method: 'POST',
+      headers: { 'Content-Type': 'application/json' },
+      body: JSON.stringify({ firstName, lastName, email, phone, province, subjects: selectedSubjects, gradeLevel, goals, marketingConsent }),
+    });
+
+    if (res.ok) {
+      btn.textContent = '✓ Request sent! We will be in touch shortly.';
+      btn.style.background = '#166534';
+      setTimeout(() => { btn.textContent = 'Book consultation →'; btn.style.background = ''; btn.disabled = false; goConsultStep(1); }, 5000);
+    } else {
+      const data = await res.json();
+      alert(data.error || 'Something went wrong. Please try again.');
+      btn.textContent = 'Book consultation →';
+      btn.disabled = false;
+    }
+  } catch (e) {
+    alert('Network error. Please check your connection and try again.');
+    btn.textContent = 'Book consultation →';
+    btn.disabled = false;
+  }
 }
 
 // ═══ PROVINCE / SUBJECT PICKER ═══
diff --git a/src/app/api/consultations/route.ts b/src/app/api/consultations/route.ts
index 5e98e0c..a80ab70 100644
--- a/src/app/api/consultations/route.ts
+++ b/src/app/api/consultations/route.ts
@@ -63,7 +63,7 @@ export async function POST(req: NextRequest) {
       email: String(email).toLowerCase().trim(),
       phone: phone ? String(phone) : null,
       province: province ? String(province) : null,
-      subjects: JSON.stringify(subjectsArray),
+      subjects: subjectsArray,
       gradeLevel: gradeLevel ? String(gradeLevel) : null,
       goals: goals ? String(goals) : null,
       marketingConsent: Boolean(marketingConsent),
@@ -81,7 +81,7 @@ export async function POST(req: NextRequest) {
       email: consultation.email,
       phone: consultation.phone,
       province: consultation.province,
-      subjects: JSON.parse(consultation.subjects) as string[],
+      subjects: consultation.subjects as string[],
       gradeLevel: consultation.gradeLevel,
       goals: consultation.goals,
     });
diff --git a/src/lib/email.ts b/src/lib/email.ts
index 5cdbfc2..5bba6ae 100644
--- a/src/lib/email.ts
+++ b/src/lib/email.ts
@@ -17,7 +17,7 @@ async function sendEmail(params: Parameters<Resend["emails"]["send"]>[0]) {
   await r.emails.send(params);
 }
 
-const FROM = process.env.FROM_EMAIL ?? "hello@mathbord.com";
+const FROM = process.env.FROM_EMAIL ?? "onboarding@resend.dev";
 const APP_URL = process.env.NEXT_PUBLIC_APP_URL ?? "http://localhost:3000";
 
 // ---------------------------------------------------------------------------
